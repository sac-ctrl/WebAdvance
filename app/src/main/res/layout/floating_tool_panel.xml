<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/fw_panel_bg"
    android:orientation="vertical">

    <!-- Sub-header: back button + tool title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@drawable/fw_header_bg"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="6dp"
        android:paddingEnd="10dp">

        <ImageButton
            android:id="@+id/toolBackButton"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@drawable/fw_window_btn_default"
            android:contentDescription="Back to web app"
            android:padding="6dp"
            android:src="@drawable/ic_baseline_arrow_back_24"
            android:tint="@color/fw_text_primary" />

        <TextView
            android:id="@+id/toolTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:singleLine="true"
            android:text="Tool"
            android:textColor="@color/fw_text_primary"
            android:textSize="15sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/toolSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:text=""
            android:textColor="@color/fw_text_secondary"
            android:textSize="11sp" />
    </LinearLayout>

    <!-- Empty-state placeholder (shown when list is empty) -->
    <TextView
        android:id="@+id/toolEmptyState"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="24dp"
        android:text="Nothing here yet."
        android:textColor="@color/fw_text_secondary"
        android:textSize="13sp"
        android:visibility="gone" />

    <!-- Scrollable list of items -->
    <ScrollView
        android:id="@+id/toolScroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true"
        android:overScrollMode="ifContentScrolls"
        android:scrollbarStyle="insideOverlay"
        android:scrollbars="vertical">

        <LinearLayout
            android:id="@+id/toolListContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="6dp"
            android:paddingBottom="10dp" />
    </ScrollView>

</LinearLayout>
