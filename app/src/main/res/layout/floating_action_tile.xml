<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tileRoot"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    android:background="@drawable/fw_action_tile_bg"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:clickable="true"
    android:focusable="true">

    <ImageView
        android:id="@+id/tileIcon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/ic_baseline_settings_black_24"
        android:tint="@color/fw_text_primary" />

    <TextView
        android:id="@+id/tileTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:singleLine="true"
        android:ellipsize="end"
        android:text="Action"
        android:textColor="@color/fw_text_primary"
        android:textSize="13sp"
        android:fontFamily="sans-serif-medium" />

    <TextView
        android:id="@+id/tileStatePill"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/fw_state_pill_off"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:paddingTop="3dp"
        android:paddingBottom="3dp"
        android:text="OFF"
        android:textColor="@color/fw_state_off_text"
        android:textSize="10sp"
        android:textStyle="bold"
        android:letterSpacing="0.08"
        android:visibility="gone" />

</LinearLayout>
