name: Build Debug APK (deprecated)

# Deprecated — superseded by debug-apk.yml.
# Kept manual-only so it doesn't fire on every push, avoiding duplicate runs.
on:
  workflow_dispatch:

jobs:
  noop:
    name: Deprecated — use 'Debug APK Build' instead
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is deprecated. The active debug build is in .github/workflows/debug-apk.yml"
